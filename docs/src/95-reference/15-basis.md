# [State bases](@id reference-basis)

An ordered set of Zeeman states drawn from the levels of a species, fixing the
index convention for matrix representations of operators: direct indexing gives
the state for an index, `stateindex` the index for a state, and `staterange` the
index block for a whole level.

```@autodocs
Modules = [Levels]
Pages = ["basis.jl"]
```

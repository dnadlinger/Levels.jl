# [Optics utilities](@id reference-optics)

Gaussian beam geometry and the intensity distribution that goes with it, used to
turn an optical power into the intensity seen by an ion.

These functions are generic over the element type, so they work with symbolic
expressions as well as unitful quantities.

```@autodocs
Modules = [Levels]
Pages = ["optics_utils.jl"]
```
